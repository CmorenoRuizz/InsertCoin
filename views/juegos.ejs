<!DOCTYPE html>
<html>
  <head>

    <%- include("partials/head") %>

  </head>
  <body>

    <header>
      <% if (user) { %>
        <%- include("partials/headerUser") %>
      <% } else { %>
        <%- include("partials/header") %>
      <% } %>
    </header>

    <main class="container mt-5">
        <h2 class="text-center mb-4">Juegos disponibles</h2>
      
        <div class="row justify-content-center">
          <div class="col-md-6">
            <div class="card text-center shadow-sm">
              <div class="card-body">
                <h5 class="card-title">Tetris</h5>
                <p class="card-text">¡Encaja las piezas y consigue la mayor puntuación posible!</p>
                <a href="/juegos/tetris" class="btn btn-primary">Jugar ahora</a>
              </div>
              <div class="card-body">
                <h5 class="card-title">Snake</h5>
                <p class="card-text">¡Come monedas y consigue la mayor puntuación posible!</p>
                <a href="/juegos/snake" class="btn btn-primary">Jugar ahora</a>
              </div>
            </div>
          </div>
        </div>
      </main>
      

    <footer>
      <%- include("partials/footer") %>
    </footer>
  </body>
</html>
